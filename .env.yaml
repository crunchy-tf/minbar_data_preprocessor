LOG_LEVEL="INFO" # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

MONGO_URI="mongodb://host.docker.internal:27017"
MONGO_DB_NAME="minbar_raw_data"
MONGO_RAW_COLLECTION="facebook_posts"
BATCH_SIZE=100
PROCESSED_STATUS_FIELD="processor_v1_processed_status"

POSTGRES_USER="postgres"                  # <-- IMPORTANT: Use the correct username for your EXISTING PostgreSQL
POSTGRES_PASSWORD="1234"  # <-- IMPORTANT: Use the correct password for your EXISTING PostgreSQL user
POSTGRES_HOST="host.docker.internal"      # <-- Connect to the HOST machine
POSTGRES_PORT="5432"                      # <-- IMPORTANT: Verify 5432 is the HOST port your PostgreSQL exposes
POSTGRES_DB="minbar_processed_data"       # <-- IMPORTANT: Make sure this database exists in your running PostgreSQL
POSTGRES_TABLE="processed_documents"      # Target table name (service will try to create if not exists)

# CAMEL_TOOLS_MLE_DISAMBIGUATOR="calima-msa-r13" # <-- REMOVED/COMMENTED OUT: No longer used

MARK_AS_PROCESSED_IN_MONGO=True # Set to false for testing runs without modifying MongoDB

SCHEDULER_INTERVAL_MINUTES=60 # e.g., 60 for hourly, 15 for every 15 minutes